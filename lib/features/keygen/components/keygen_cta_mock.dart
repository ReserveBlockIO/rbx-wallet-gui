import 'package:flutter/material.dart';

// This is for when running native (non web)

class KeygenCta extends StatelessWidget {
  const KeygenCta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
